{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "font-sniper",
  "title": "Font Sniper",
  "description": "Extract and download fonts from any website",
  "icon": "extension-icon.png",
  "author": "raycast",
  "categories": ["Developer Tools", "Web"],
  "license": "MIT",
  "commands": [
    {
      "name": "extract-fonts",
      "title": "Download Fonts",
      "description": "Extract and download fonts from a website URL",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "showWoff2",
      "title": "Show WOFF2 Fonts",
      "description": "Include WOFF2 format fonts in results",
      "type": "checkbox",
      "default": true,
      "required": false
    },
    {
      "name": "showWoff",
      "title": "Show WOFF Fonts",
      "description": "Include WOFF format fonts in results",
      "type": "checkbox",
      "default": true,
      "required": false
    },
    {
      "name": "showTtf",
      "title": "Show TTF Fonts",
      "description": "Include TTF format fonts in results",
      "type": "checkbox",
      "default": true,
      "required": false
    },
    {
      "name": "showOtf",
      "title": "Show OTF Fonts",
      "description": "Include OTF format fonts in results",
      "type": "checkbox",
      "default": true,
      "required": false
    },
    {
      "name": "showEot",
      "title": "Show EOT Fonts",
      "description": "Include EOT format fonts in results",
      "type": "checkbox",
      "default": false,
      "required": false
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.56.0"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "20.8.10",
    "@types/react": "18.2.27",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "npx @raycast/api@latest publish"
  }
}
